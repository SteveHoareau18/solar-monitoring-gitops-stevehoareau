apiVersion: apps/v1
kind: Deployment
metadata:
  name: solar-simulator
  namespace: solar-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: solar-simulator
  template:
    metadata:
      labels:
        app: solar-simulator
    spec:
      containers:
      - name: simulator
        image: solar-simulator:latest
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
